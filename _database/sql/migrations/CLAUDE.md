# SQL Migrations

Read and follow [AGENTS.md](AGENTS.md) in this directory.

This must remain a regular file. Diesel treats a symlink here as a migration
directory and fails with `Invalid migration directory`.
